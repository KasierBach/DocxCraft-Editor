# Security policy

## Deployment boundary

The API is local-only by default and has no authentication. Do not expose it to a network, reverse proxy, or shared host until authentication, authorization, HTTPS, and rate limiting are configured.

## Reporting

Do not open a public issue for a suspected vulnerability. Report it privately to the repository owner with reproduction steps, impact, and the affected commit.
